//Write a function that takes two numbers as arguments and returns their sum.
//Write both a regular function and an arrow function.

function add(a, b) {
    return a + b;
  }
  
  const addArrow = (a, b) => a + b;
  
  console.log(add(5, 10)); // 15
  console.log(addArrow(5, 10)); // 15
 
  
